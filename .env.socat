# VPN Socat Relay Configuration
# This file configures automatic localhost-to-VPN port relays using socat
# Each port listed here will be relayed from VPN interface to localhost

# Socat Configuration
SOCAT_VPN_IP=10.8.0.6                 # Your VPN client IP (auto-detected if empty)
SOCAT_LOCALHOST_IP=127.0.0.1          # Localhost IP for relay destination

# Port Relay Configuration
# Format: PORT:DESCRIPTION (one per line, comma separated)
# Example: 3000:HTTP_Development_Server,8080:Alternative_HTTP,9000:Backend_API
SOCAT_PORTS="3000:HTTP_Dev_Server,8080:Alt_HTTP,9000:Backend_API,5432:PostgreSQL_DB"

# Service Configuration  
SOCAT_RESTART_POLICY=always           # always, on-failure, on-abnormal, on-abort, on-watchdog, never
SOCAT_RESTART_DELAY=10                # Seconds to wait before restart
SOCAT_USER=nobody                     # User to run socat as (root for ports < 1024)

# Logging Configuration
SOCAT_LOG_LEVEL=notice                # fatal, error, warning, notice, info, debug
SOCAT_ENABLE_LOGGING=true             # Enable detailed logging to journal

# Advanced Options
SOCAT_FORK=true                       # Enable fork mode for multiple connections
SOCAT_REUSEADDR=true                  # Enable SO_REUSEADDR socket option
SOCAT_KEEPALIVE=false                 # Enable TCP keepalive (experimental)

# UFW Firewall Integration
SOCAT_AUTO_UFW_RULES=true             # Automatically suggest UFW rules
SOCAT_UFW_COMMENT_PREFIX="VPN Relay"  # Comment prefix for UFW rules