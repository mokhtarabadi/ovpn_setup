# OpenVPN Server Configuration
# Copy this file to .env and customize the values

# Protocol for OpenVPN server (udp or tcp)
# UDP: Faster, better for gaming/streaming, default for OpenVPN
# TCP: More reliable, better for restrictive networks/firewalls
OPENVPN_PROTOCOL=udp

# Port for OpenVPN server (default: 1194 for UDP, 443 for TCP)
OPENVPN_PORT=1194

# Your server's public domain or IP address
# This is what clients will connect to
VPN_DOMAIN=your-server.example.com

# VPN network settings
VPN_NETWORK=10.8.0.0
VPN_SERVER_IP=10.8.0.1

# Allow multiple connections per certificate (duplicate-cn)
# true: One certificate can be used on multiple devices simultaneously
#       - Convenient for users with phone, laptop, desktop, etc.
#       - Higher security risk if one device is compromised
#       - Cannot revoke individual devices
# false: One connection per certificate (default OpenVPN behavior)
#        - More secure, can revoke individual device access
#        - Need separate certificates for each device
ALLOW_DUPLICATE_CN=false

# Alternative configurations for different use cases:
# For restrictive networks (TCP + port 443):
# OPENVPN_PROTOCOL=tcp
# OPENVPN_PORT=443

# For gaming/streaming (UDP + port 1194 - default):
# OPENVPN_PROTOCOL=udp
# OPENVPN_PORT=1194

# Docker-compose project name (optional)
# COMPOSE_PROJECT_NAME=openvpn